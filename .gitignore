# Secrets -- never commit
.env

# Working notes and planning docs -- scaffolding, kept out of the public repo
CLAUDE.md
KICKSDB_INTEGRATION_PLAN.md
KICKSDB_PROGRESS.md
ML_PIPELINE_PROJECT_PLAN.md
DASHBOARD_REWORK_PLAN.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Test / lint / type caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Local pipeline data + model artifacts (regenerated from fixtures)
data/raw/
data/features/
data/validated/
data/models/
data/failures/
data/predictions/
data/drift/
*.parquet
*.pt
mlruns/
mlartifacts/
mlflow.db
ray_results/

# Allow the committed fixtures directory to exist even though *.parquet is
# ignored above -- fixtures are regenerated by generate_fixtures.py, so we keep
# the folder but not its contents.
!data/fixtures/

# Airflow local state
airflow.db
airflow.cfg
logs/
standalone_admin_password.txt

# Terraform (never commit state or real tfvars; the lock file IS committed)
infra/terraform/.terraform/
infra/terraform/*.tfstate
infra/terraform/*.tfstate.*
infra/terraform/*.tfvars
crash.log

# Demo live snapshot -- generated by the refresh job and published straight to
# the Space; the demo falls back to canned records when it's absent.
demo/live_snapshot.json
demo/drift_history.json

# Scaffolded retail-reference draft -- reviewed by hand, then merged into the
# real reference; not committed as-is.
config/kicksdb_retail_reference.draft.json

# OS / editor
.DS_Store
.idea/
.vscode/
